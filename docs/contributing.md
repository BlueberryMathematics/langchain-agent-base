# Contributing to LangChain Agent Base

Thank you for your interest in contributing! This guide will help you get started with contributing to the LangChain Agent Base project.

## 🎯 Project Goals

The LangChain Agent Base aims to be:
- **Production-Ready**: Reliable, well-tested code for real applications
- **Easy to Use**: Simple imports and intuitive APIs
- **Extensible**: Easy to add new tools, agents, and features
- **Well-Documented**: Comprehensive guides and examples
- **Fast**: Optimized for performance with Groq integration

## 🤝 How to Contribute

### Types of Contributions

1. **🐛 Bug Reports**: Found an issue? Report it!
2. **✨ Feature Requests**: Have an idea? Suggest it!
3. **🔧 Tool Additions**: Add new tools for agents
4. **📚 Documentation**: Improve guides and examples
5. **🧪 Tests**: Add or improve test coverage
6. **🔄 Code Improvements**: Refactoring and optimizations

### Getting Started

1. **Fork the Repository**
   ```bash
   git clone https://github.com/your-username/langchain-agent-base.git
   cd langchain-agent-base
   ```

2. **Set Up Development Environment**
   ```bash
   # Create virtual environment
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\\Scripts\\activate
   
   # Install dependencies
   pip install -r requirements.txt
   pip install -e .  # Install in development mode
   
   # Install development dependencies
   pip install pytest black flake8 mypy
   ```

3. **Set Up Environment Variables**
   ```bash\n   cp .env.example .env\n   # Edit .env with your API keys\n   export GROQ_API_KEY=\"your_groq_key\"\n   ```\n\n4. **Create a Branch**\n   ```bash\n   git checkout -b feature/your-feature-name\n   ```\n\n## 🔧 Adding New Tools\n\nTools are the core extensibility point of the system. Here's how to add new tools:\n\n### Basic Tool Structure\n\n```python\nfrom langchain_core.tools import tool\nfrom typing import Optional\n\n@tool\ndef your_tool_name(parameter: str, optional_param: Optional[int] = None) -> str:\n    \"\"\"Brief description of what the tool does.\n    \n    Args:\n        parameter: Description of the required parameter\n        optional_param: Description of optional parameter\n    \n    Returns:\n        Description of what the tool returns\n    \n    Example:\n        >>> your_tool_name(\"example\")\n        \"Expected output\"\n    \"\"\"\n    try:\n        # Tool implementation\n        result = f\"Processed: {parameter}\"\n        \n        if optional_param:\n            result += f\" with value {optional_param}\"\n        \n        return result\n        \n    except Exception as e:\n        return f\"Error in your_tool_name: {str(e)}\"\n```\n\n### Tool Categories\n\nAdd tools to the appropriate category in `src/tools.py`:\n\n```python\ndef get_your_category_tools():\n    \"\"\"Get tools for your specific domain.\"\"\"\n    return [\n        your_tool_name,\n        another_tool_in_category,\n        # ... more tools\n    ]\n\n# Update get_all_tools() to include your category\ndef get_all_tools():\n    \"\"\"Get all available tools.\"\"\"\n    all_tools = []\n    all_tools.extend(get_basic_tools())\n    all_tools.extend(get_math_tools())\n    all_tools.extend(get_science_tools())\n    all_tools.extend(get_coding_tools())\n    all_tools.extend(get_your_category_tools())  # Add this line\n    return all_tools\n```\n\n### Adding Commands for Tools\n\nIf your tool should be available as a command, add it to `src/commands.py`:\n\n```python\nfrom src.commands import command\n\n@command(\"your_cmd\", \"Description of command\", \"/your_cmd <param>\")\ndef your_command(parameter: str, optional_param: int = None) -> str:\n    \"\"\"Command version of your tool.\"\"\"\n    return your_tool_name(parameter, optional_param)\n```\n\n### Tool Development Guidelines\n\n1. **Error Handling**: Always include proper error handling\n2. **Type Hints**: Use complete type hints for all parameters\n3. **Documentation**: Include comprehensive docstrings with examples\n4. **Validation**: Validate inputs and provide helpful error messages\n5. **Testing**: Add tests for your tools (see Testing section)\n\n## 🤖 Adding New Agent Types\n\nTo add new specialized agent types:\n\n### Factory Function Pattern\n\n```python\n# Add to src/base.py\n\ndef create_your_domain_agent(enable_commands: bool = True, **kwargs) -> Agent:\n    \"\"\"Create agent specialized for your domain.\n    \n    Args:\n        enable_commands: Whether to enable command system\n        **kwargs: Additional arguments passed to Agent constructor\n    \n    Returns:\n        Configured agent for your domain\n    \n    Example:\n        >>> agent = create_your_domain_agent()\n        >>> response = agent.chat(\"Domain-specific question\")\n    \"\"\"\n    # Custom system prompt for your domain\n    system_prompt = \"\"\"You are an expert in [your domain]. You excel at:\n    - Specific capability 1\n    - Specific capability 2\n    - Specific capability 3\n    \n    Always provide accurate, helpful information in your domain.\"\"\"\n    \n    # Create agent with domain-specific tools\n    agent = Agent(\n        system_prompt=system_prompt,\n        **kwargs\n    )\n    \n    # Add domain-specific tools\n    agent.add_tools(get_your_domain_tools())\n    \n    # Enable commands if requested\n    if enable_commands:\n        agent.enable_commands()\n    \n    return agent\n```\n\n### Agent Class Extension\n\nFor more complex agent behaviors:\n\n```python\nclass YourSpecializedAgent(Agent):\n    \"\"\"Specialized agent for specific use cases.\"\"\"\n    \n    def __init__(self, special_param: str, **kwargs):\n        super().__init__(\n            system_prompt=f\"Specialized agent with {special_param}\",\n            **kwargs\n        )\n        self.special_param = special_param\n        self._setup_specialized_behavior()\n    \n    def _setup_specialized_behavior(self):\n        \"\"\"Set up specialized behavior for this agent.\"\"\"\n        # Add specialized tools\n        self.add_tools(get_specialized_tools())\n        \n        # Configure specialized settings\n        self.enable_commands()\n    \n    def specialized_method(self, input_data: str) -> str:\n        \"\"\"Specialized method for this agent type.\"\"\"\n        return f\"Specialized processing of {input_data} with {self.special_param}\"\n```\n\n## 📝 Documentation Guidelines\n\n### Code Documentation\n\n1. **Docstrings**: Use Google-style docstrings\n2. **Type Hints**: Include complete type annotations\n3. **Examples**: Provide usage examples in docstrings\n4. **Comments**: Explain complex logic with inline comments\n\n### User Documentation\n\n1. **Getting Started**: Update guides for new features\n2. **API Reference**: Document new classes and methods\n3. **Examples**: Add real-world usage examples\n4. **Tutorials**: Create step-by-step guides\n\n### Documentation Structure\n\n```markdown\n# Feature Name\n\nBrief description of the feature.\n\n## Usage\n\nBasic usage example:\n\n```python\ncode example\n```\n\n## Advanced Usage\n\nMore complex examples:\n\n```python\nadvanced code example\n```\n\n## API Reference\n\nDetailed parameter descriptions.\n```\n\n## 🧪 Testing\n\n### Writing Tests\n\nCreate tests in the `tests/` directory:\n\n```python\n# tests/test_your_feature.py\nimport pytest\nfrom src.base import Agent\nfrom src.tools import your_tool_name\n\nclass TestYourFeature:\n    \"\"\"Test suite for your feature.\"\"\"\n    \n    def test_basic_functionality(self):\n        \"\"\"Test basic functionality.\"\"\"\n        result = your_tool_name(\"test input\")\n        assert \"Expected\" in result\n    \n    def test_error_handling(self):\n        \"\"\"Test error handling.\"\"\"\n        result = your_tool_name(\"\")  # Invalid input\n        assert \"Error\" in result\n    \n    def test_agent_integration(self):\n        \"\"\"Test tool works with agent.\"\"\"\n        agent = Agent()\n        agent.add_tool(your_tool_name)\n        \n        response = agent.chat(\"Use the tool with test input\")\n        assert response is not None\n\n@pytest.fixture\ndef sample_agent():\n    \"\"\"Fixture for testing.\"\"\"\n    agent = Agent()\n    agent.add_tool(your_tool_name)\n    return agent\n```\n\n### Running Tests\n\n```bash\n# Run all tests\npytest\n\n# Run specific test file\npytest tests/test_your_feature.py\n\n# Run with coverage\npytest --cov=src\n\n# Run specific test\npytest tests/test_your_feature.py::TestYourFeature::test_basic_functionality\n```\n\n### Test Coverage Guidelines\n\n- **New Tools**: 100% coverage for tool functions\n- **New Agents**: Test creation, tool addition, basic chat\n- **Error Cases**: Test error handling and edge cases\n- **Integration**: Test with existing system components\n\n## 🎨 Code Style\n\n### Python Style Guide\n\n1. **PEP 8**: Follow PEP 8 style guidelines\n2. **Black**: Use Black for code formatting\n3. **Flake8**: Use Flake8 for linting\n4. **MyPy**: Use MyPy for type checking\n\n### Pre-commit Hooks\n\nSet up pre-commit hooks:\n\n```bash\n# Install pre-commit\npip install pre-commit\n\n# Set up hooks\npre-commit install\n\n# Run hooks manually\npre-commit run --all-files\n```\n\n### Code Formatting\n\n```bash\n# Format code with Black\nblack src/ tests/\n\n# Check with Flake8\nflake8 src/ tests/\n\n# Type check with MyPy\nmypy src/\n```\n\n## 🔄 Pull Request Process\n\n### Before Submitting\n\n1. **Run Tests**: Ensure all tests pass\n2. **Code Style**: Format code and fix linting issues\n3. **Documentation**: Update relevant documentation\n4. **Examples**: Add usage examples if needed\n\n### Pull Request Template\n\n```markdown\n## Description\n\nBrief description of changes.\n\n## Type of Change\n\n- [ ] Bug fix\n- [ ] New feature\n- [ ] Documentation update\n- [ ] Performance improvement\n- [ ] Other (please describe)\n\n## Testing\n\n- [ ] Tests added/updated\n- [ ] All tests pass\n- [ ] Manual testing completed\n\n## Documentation\n\n- [ ] Documentation updated\n- [ ] Examples added/updated\n- [ ] API reference updated\n\n## Checklist\n\n- [ ] Code follows style guidelines\n- [ ] Self-review completed\n- [ ] No breaking changes (or clearly documented)\n- [ ] Performance impact considered\n```\n\n### Review Process\n\n1. **Automated Checks**: CI/CD runs tests and style checks\n2. **Code Review**: Maintainers review code and provide feedback\n3. **Iteration**: Address feedback and update PR\n4. **Approval**: Two maintainer approvals required\n5. **Merge**: Maintainer merges after approval\n\n## 🐛 Bug Reports\n\n### Bug Report Template\n\n```markdown\n**Bug Description**\nClear description of the bug.\n\n**Steps to Reproduce**\n1. Step one\n2. Step two\n3. ...\n\n**Expected Behavior**\nWhat should happen.\n\n**Actual Behavior**\nWhat actually happens.\n\n**Environment**\n- OS: [e.g., Windows 10, macOS 12.0, Ubuntu 20.04]\n- Python Version: [e.g., 3.11.0]\n- LangChain Version: [e.g., 1.0.8]\n- Groq Version: [e.g., 0.4.1]\n\n**Additional Context**\nAny other relevant information.\n```\n\n## ✨ Feature Requests\n\n### Feature Request Template\n\n```markdown\n**Feature Description**\nClear description of the proposed feature.\n\n**Use Case**\nWhy is this feature needed? What problem does it solve?\n\n**Proposed Solution**\nHow should this feature work?\n\n**Alternatives Considered**\nOther approaches you've considered.\n\n**Implementation Ideas**\nAny thoughts on how to implement this.\n```\n\n## 🏗️ Architecture Guidelines\n\n### Project Structure\n\n```\nsrc/\n├── base.py          # Core agent classes and factories\n├── tools.py         # Tool collections and implementations\n├── rag.py           # RAG system and document processing\n├── commands.py      # Command system and registry\n└── utils.py         # Utility functions and helpers\n```\n\n### Design Principles\n\n1. **Modularity**: Each component should be independently usable\n2. **Extensibility**: Easy to add new tools, agents, and features\n3. **Simplicity**: Simple APIs with sensible defaults\n4. **Performance**: Optimize for speed and memory usage\n5. **Reliability**: Robust error handling and graceful degradation\n\n### Compatibility\n\n- **Python 3.11+**: Minimum Python version\n- **LangChain 1.0+**: Use latest LangChain patterns\n- **Backward Compatibility**: Don't break existing APIs\n- **Cross-Platform**: Support Windows, macOS, and Linux\n\n## 🎉 Recognition\n\n### Contributors\n\nAll contributors are recognized in:\n- README.md contributors section\n- Release notes for significant contributions\n- GitHub contributors page\n\n### Types of Recognition\n\n- **Code Contributors**: Authors of merged pull requests\n- **Documentation Contributors**: Improve guides and examples\n- **Bug Reporters**: Help identify and fix issues\n- **Feature Requesters**: Suggest valuable improvements\n- **Community Members**: Help others in discussions\n\n## 📞 Getting Help\n\n### Channels\n\n1. **GitHub Issues**: For bugs and feature requests\n2. **GitHub Discussions**: For questions and general discussion\n3. **Documentation**: Check existing guides and examples\n4. **Code Examples**: Review the examples directory\n\n### Response Times\n\n- **Bug Reports**: 24-48 hours initial response\n- **Feature Requests**: 1-3 days initial response\n- **Pull Requests**: 1-5 days review turnaround\n- **Questions**: Best effort, usually within a few days\n\n---\n\n**Thank you for contributing to LangChain Agent Base! Your contributions help make AI agents more accessible and powerful for everyone.** 🚀