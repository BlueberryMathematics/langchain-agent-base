# Project Architecture Diagram

This document shows how all the pieces fit together in your Ultimate Math Agent project.

## 🏗️ **Complete Project Architecture**

```
┌─────────────────────────────────────────────────────────────────┐
│                     YOUR PROJECT REPOSITORY                      │
│                   (my-ultimate-math-agent)                       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐    ┌────────────────┐    ┌────────────────┐
│   src/        │    │ custom_tools/  │    │   agents/      │
│  (Framework)  │    │ (Your Tools)   │    │ (Your Agent)   │
└───────────────┘    └────────────────┘    └────────────────┘
        │                     │                     │
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐    ┌────────────────┐    ┌────────────────┐
│ • base.py     │    │ • calculus.py  │    │ • math_agent.py│
│ • tools.py    │    │ • statistics.py│    │                │
│ • memory.py   │    │ • algebra.py   │    │   Uses:        │
│ • protocol.py │    │ • __init__.py  │    │   - Framework  │
│ • storage.py  │    │                │    │   - Built-in   │
│ • rag.py      │    │  Each has      │    │   - Custom     │
│ • server.py   │    │  @tool funcs   │    └────────────────┘
│ • discovery.py│    └────────────────┘              │
│ • commands.py │                                    │
└───────────────┘                                    │
        │                                            │
        │         ┌──────────────────────────────────┘
        │         │
        ▼         ▼
┌──────────────────────────────────────┐
│     YOUR ULTIMATE MATH AGENT         │
│                                       │
│  Capabilities:                        │
│  ✓ Built-in tools (calculator, etc)  │
│  ✓ Custom tools (derivatives, etc)   │
│  ✓ Conversation memory                │
│  ✓ Command system                     │
│  ✓ REST API                           │
│  ✓ Auto-discovery                     │
└──────────────────────────────────────┘
```

## 🔄 **Data Flow: How a Query Works**

```
User Query: "Calculate the derivative of x^2 + 3x"
    │
    ▼
┌─────────────────────┐
│  Agent (base.py)    │ ← Your agent inherits from this
│  - Receives message │
│  - Has memory       │
│  - Knows all tools  │
└─────────────────────┘
    │
    │ Analyzes query
    │
    ▼
┌─────────────────────┐
│  Tool Selection     │
│  - Scans available  │
│  - Matches intent   │
│  - Picks best tool  │
└─────────────────────┘
    │
    │ Selected: derivative tool
    │
    ▼
┌──────────────────────┐
│ derivative()         │ ← Your custom tool!
│ from calculus.py     │
│ - Receives params    │
│ - Does calculation   │
│ - Returns result     │
└──────────────────────┘
    │
    │ Returns: "d/dx(x^2+3x) = 2x+3"
    │
    ▼
┌─────────────────────┐
│  Memory System      │
│  - Stores query     │
│  - Stores response  │
│  - Indexes for      │
│    future search    │
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│  Response to User   │
│  "The derivative    │
│   is 2x + 3"        │
└─────────────────────┘
```

## 🧩 **Component Relationships**

```
                    ┌──────────────────────┐
                    │  External Services   │
                    │  • Groq API          │
                    │  • Qdrant Database   │
                    └──────────┬───────────┘
                               │
                               │
        ┌──────────────────────┴─────────────────────┐
        │          LangChain Agent Base              │
        │              (Framework)                   │
        └──────────────────────┬─────────────────────┘
                               │
        ┌──────────────────────┼─────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│ Agent Class  │      │ Memory System│      │ Tool System  │
│              │      │              │      │              │
│ • Chat loop  │◄────►│ • Storage    │◄────►│ • Registry   │
│ • Tool calls │      │ • Retrieval  │      │ • Execution  │
│ • Commands   │      │ • Summary    │      │ • Discovery  │
└──────┬───────┘      └──────────────┘      └──────┬───────┘
       │                                             │
       │         ┌───────────────────┐              │
       └────────►│   Your Agent      │◄─────────────┘
                 │                   │
                 │ Combines:         │
                 │ • Framework       │
                 │ • Built-in tools  │
                 │ • Custom tools    │
                 └───────────────────┘
```

## 📦 **Tool Integration Pattern**

```
1. You Write:
   ┌─────────────────────┐
   │ @tool               │
   │ def my_tool(x):     │
   │     return result   │
   └─────────────────────┘
              │
              │ Export in __init__.py
              ▼
   ┌─────────────────────┐
   │ get_custom_tools()  │
   │   return [my_tool]  │
   └─────────────────────┘
              │
              │ Import in agent
              ▼
   ┌─────────────────────┐
   │ agent.add_tools(    │
   │   get_custom_tools()│
   │ )                   │
   └─────────────────────┘
              │
              │ Framework registers
              ▼
   ┌─────────────────────┐
   │ Tool is now:        │
   │ • In agent          │
   │ • In API            │
   │ • In discovery      │
   │ • In commands       │
   └─────────────────────┘
```

## 🚀 **Development Workflow**

```
Day 1: Setup
┌─────────────────────┐
│ 1. Copy framework   │
│ 2. Install deps     │
│ 3. Set API key      │
│ 4. Create 1 tool    │
│ 5. Create agent     │
│ 6. Test it!         │
└─────────────────────┘
         │
         │ It works!
         ▼
Day 2-7: Add Tools
┌─────────────────────┐
│ Monday: Derivative  │
│ Tuesday: Integral   │
│ Wednesday: Limit    │
│ Thursday: Stats     │
│ Friday: Matrix ops  │
└─────────────────────┘
         │
         │ Growing nicely
         ▼
Week 2: Features
┌─────────────────────┐
│ • Enable memory     │
│ • Add commands      │
│ • Test thoroughly   │
└─────────────────────┘
         │
         │ Ready for users
         ▼
Week 3: Deploy
┌─────────────────────┐
│ • Start API server  │
│ • Document usage    │
│ • Share with users  │
└─────────────────────┘
         │
         │ In production!
         ▼
Ongoing: Maintenance
┌─────────────────────┐
│ • Add tools as      │
│   needed            │
│ • Fix bugs          │
│ • Improve prompts   │
└─────────────────────┘
```

## 💾 **Memory System Architecture**

```
User chats with agent...

Chat: "My name is Alice"
    │
    ▼
┌──────────────────────────┐
│ ConversationMemoryManager│
│ • Receives message       │
│ • Stores in Qdrant       │
│ • Creates embedding      │
└──────────────────────────┘
    │
    ▼
┌──────────────────────────┐
│ Qdrant Database          │
│ Collection: conversations│
│ • Text: "My name is Alice│
│ • Vector: [0.1, 0.5, ...│
│ • Metadata: timestamp    │
└──────────────────────────┘

Later...

Chat: "What's my name?"
    │
    ▼
┌──────────────────────────┐
│ Memory Search            │
│ • Creates query embedding│
│ • Searches Qdrant        │
│ • Finds: "Alice" message │
└──────────────────────────┘
    │
    ▼
Agent: "Your name is Alice"
```

## 🌐 **API Server Architecture** (Optional)

```
When you run: python -m src.server

┌─────────────────────────┐
│   FastAPI Server        │
│   (Auto-generated)      │
└───────────┬─────────────┘
            │
    ┌───────┼───────┐
    │       │       │
    ▼       ▼       ▼
┌──────┐ ┌────┐ ┌────┐
│ GET  │ │POST│ │GET │
│/agents│/chat│/tools│
└──────┘ └────┘ └────┘
    │
    │ All routes automatically
    │ created from your agent
    │
    ▼
┌─────────────────────────┐
│   Your Ultimate         │
│   Math Agent            │
│   (Same one you         │
│    use in Python!)      │
└─────────────────────────┘

Access via:
• http://localhost:8000/docs (Swagger UI)
• http://localhost:8000/chat (Chat endpoint)
• http://localhost:8000/agents (List agents)
```

## 🎯 **The Key Insight**

```
┌─────────────────────────────────────────┐
│  You Write:                              │
│  • Tools (@tool functions)               │
│  • Agent config (system prompt + tools)  │
│                                          │
│  Framework Provides:                     │
│  • Agent loop ✓                          │
│  • Memory ✓                              │
│  • Tool calling ✓                        │
│  • API generation ✓                      │
│  • Discovery ✓                           │
│  • Commands ✓                            │
│  • Error handling ✓                      │
│  • Versioning ✓                          │
│                                          │
│  You focus on WHAT, not HOW!             │
└─────────────────────────────────────────┘
```

## ✅ **Summary: Three Layers**

```
┌────────────────────────────────────┐
│  Layer 3: Your Application         │
│  • Custom tools                    │
│  • Agent configuration             │
│  • Business logic                  │
└────────────┬───────────────────────┘
             │
┌────────────▼───────────────────────┐
│  Layer 2: LangChain Agent Base     │
│  • Agent class                     │
│  • Memory system                   │
│  • Tool management                 │
│  • API generation                  │
└────────────┬───────────────────────┘
             │
┌────────────▼───────────────────────┐
│  Layer 1: External Services        │
│  • Groq (LLM)                      │
│  • Qdrant (Storage)                │
│  • LangChain (Framework)           │
└────────────────────────────────────┘
```

**You work in Layer 3. Layers 1 and 2 are handled for you!**

---

This architecture allows you to:
- ✅ Start simple (1 tool)
- ✅ Grow naturally (add tools over time)
- ✅ Never rewrite (same structure scales)
- ✅ Deploy easily (API auto-generated)
- ✅ Focus on value (your domain expertise)

**The framework does the heavy lifting, you do the creative work!** 🚀
